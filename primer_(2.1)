#include <iostream>
using namespace std;
float sum(float a, float b, float c);
int main()
{
float a,b,c;
cout << "vvedite tri chisla:";
cin >> a >> b >> c;
cout << "sum=" <<a+b+c;
return 0;
}
float sum(float a, float b, float c)
{return a+b+c;}
